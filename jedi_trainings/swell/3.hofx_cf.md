# Running `hofx_cf` suite

To run the defualt `hofx_cf` suite (without any additional input) follow these steps:

1. Load the SWELL environment 

2. Create the exepriment by running `swell create hofx_cf`.
This will print out:

```bash
SwellCreateExperiment:  
SwellCreateExperiment: Experiment successfully installed. To launch experiment use: 
SwellCreateExperiment:  
SwellCreateExperiment:   swell launch /discover/nobackup/mabdiosk/SwellExperiments/swell-hofx_cf/swell-hofx_cf-suite
SwellCreateExperiment:  
```


3. Launch your experiment by running `swell launch <path_to_experiment.yaml>`. You can find this path in the output log of the previous step.

`cyclc scan` will show the name of the active cylc experiment. 
You can view the active exepriment by `running cylc tui <experiment_name>`

With cylc tui you can view the tasks as they get queued and executed in real time. 

## Tasks in `hofx_cf` suite

All the tasks and their depenendencies are defined in [`src/swell/suites/hofx_cf/flow.cylc`](https://github.com/GEOS-ESM/swell/blob/develop/src/swell/suites/hofx_cf/flow.cylc). Let's take a closer look.

Here's the list of the tasks in `hofx_cf`:

- BuildJediByLinking
- GetBackground
- GetObservations
- StageJediCycle
- RenderJediObservations
- RunJediHofxExecutable
- EvaObservations
- CleanCycle

...Something about defining dependencies in cyclc with =>

...something about what each task does

## Exploring log and run directories
...Where to find the run directory
... where to find the log directory

## What are the final outputs
Where to find the feedback files, they are stored on R2D2
How to fetch them later? using r2d2_util command that has `r2d2.fetch in it.


